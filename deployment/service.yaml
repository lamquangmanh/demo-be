apiVersion: v1
kind: Service
metadata:
  name: core-be-service
spec:
  type: LoadBalancer # or NodePort
  selector:
    app: core-be
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
